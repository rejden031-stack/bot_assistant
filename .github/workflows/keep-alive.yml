name: Keep Render alive

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health endpoint
        run: curl -s -o /dev/null -w "%{http_code}" https://bot-assistant-vhbf.onrender.com/health
